exports.command = function(selector, expected, message) {
    var self = this;
    self.getText(selector, function(result) {
            self.showTest(result.value === expected, result.value, expected, message);
        });
};

